name: taworld_notifications
description: TaWorld Android reminders with a durable local evidence inbox.
version: 0.1.0
publish_to: none
environment:
  sdk: '>=3.6.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
flutter:
  plugin:
    platforms:
      android:
        package: com.taworld.notifications
        pluginClass: TaNotificationPlugin
